# Binary backwards compatibility tests module

 * Code to compile against the previous version goes into src/main.
 * Test code goes into src/test which will have the current version on the classpath.